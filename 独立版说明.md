# 无限画布 · 独立版

双击 `启动独立画布.command`，打开 http://127.0.0.1:3102/canvas 。关闭启动终端或按 Ctrl+C 停止本次启动的服务。重复启动遇到端口占用时不会终止其他进程，直接打开上述地址即可。

- 本项目是基于上游无限画布整理的独立版本；更新通过本项目的 Git 仓库分发，不会自动覆盖上游或同步上游改动。
- 原版仍使用 3101；新版前端使用 3102，Agent 使用 17375。Agent 配置、Token、工作空间和消息元数据保存在 `canvas-agent/.runtime/`，不要公开该目录。
- 新旧版本的浏览器项目、素材、配置按端口隔离。需要已有内容时，从原版导出画布，再到新版导入；API 配置需在新版设置中配置。不复制原版 Token 或会话。
- 仍使用本机 Codex 登录和模型能力；没有额外安装或替换 Codex 插件。请用本启动器启动新版，不用原版插件的“打开画布”命令启动它。
- `＋` 添加文字、图片、视频、音频；专业模式额外显示生成配置、分组和已安装插件。切换模式不删除节点。
- `⌘/Ctrl+K` 搜索定位；`Shift+1` 适应全部节点；`⇧⌘/Ctrl+F` 切换专注模式，Esc 退出。
- “自动整理节点”按连线关系分列，分组整体移动，可用撤销恢复；不改变节点内容、引用和连线。
- 新版不包含 TOPAPIS 品牌、云分享、积分和第三方画布导入，也没有更换为 Leafer 引擎。

上游 MIT 许可证保留在 `LICENSE`。后续改动只在此目录进行，不对原目录执行覆盖或同步。

本仓库还包含受非商业授权约束的 AIFISHER 来源内容，使用前请先阅读 [授权说明](README.md#授权说明) 和随附许可证。
